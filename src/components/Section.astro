---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<section
  class='w-full flex flex-col items-center py-14 gap-y-5 container mx-auto max-sm:px-4'
>
  {title && <h1 class='text-2xl font-bold'>{title}</h1>}
  <slot />
</section>
