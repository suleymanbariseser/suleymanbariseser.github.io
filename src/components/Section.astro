---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<div class='w-full flex flex-col items-center py-14 gap-y-5 container mx-auto'>
  {title && <h1 class='text-2xl font-bold'>{title}</h1>}
  <slot />
</div>
